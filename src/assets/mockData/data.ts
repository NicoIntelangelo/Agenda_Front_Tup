import { Contact, defaultContact } from "src/app/interfaces/contactData";

export const contactsData:Contact[] = [
    defaultContact,
    defaultContact,
    defaultContact,
    defaultContact,
    defaultContact,
    defaultContact,
]
